---
inclusion: manual
---

# 提交前代码检查

当用户输入"检查本次修改代码是否可以提交"或类似指令时，对本次会话中所有修改过的文件逐一进行以下检查：

## 检查项

### 1. Mock 数据残留
- 检查是否存在 `// TODO: mock数据` 相关注释
- 检查是否存在硬编码的 mock 数据赋值（如直接给变量赋固定假数据）
- 检查是否存在 `mock` 关键字（不区分大小写，排除文件名或路径中的合理引用）

### 2. TODO 标记残留
- 检查是否存在 `TODO`、`FIXME`、`HACK`、`XXX` 等标记注释
- 列出每一处 TODO 的具体位置和内容，由用户判断是否可以提交

### 3. localhost 调用
- 检查是否存在 `localhost`、`127.0.0.1`、`0.0.0.0` 等本地地址引用
- 检查是否存在硬编码的本地端口调用（如 `http://localhost:8080`）

## 输出格式

对每个修改过的文件，按以下格式输出检查结果：

```
📄 文件名: xxx
  ✅ Mock 数据检查通过 / ❌ 发现 Mock 数据残留（第 X 行：具体内容）
  ✅ TODO 检查通过 / ❌ 发现 TODO 标记（第 X 行：具体内容）
  ✅ localhost 检查通过 / ❌ 发现 localhost 调用（第 X 行：具体内容）
```

最终给出汇总结论：
- ✅ 所有检查通过，可以提交
- ❌ 存在以下问题需要处理后再提交（列出问题清单）
