---
inclusion: manual
---

# 项目 Steering 初始化

当用户要求"初始化项目 steering"或类似指令时，按照以下流程生成项目的 steering 文档。

## 前置条件

- 用户必须提供一个**关键字**（如项目名称缩写），用于作为生成文件的前缀
- 如果用户未提供关键字，**必须先提示用户输入，等用户回复后再执行**，禁止自行猜测或使用默认值
- 所有生成的文件放在 `.kiro/steering/` 目录下

## 文件命名规则（严格遵守）

生成的文件**必须且只能**是以下四个，文件名使用用户提供的关键字作为前缀：

1. `.kiro/steering/{关键字}-product.md`
2. `.kiro/steering/{关键字}-structure.md`
3. `.kiro/steering/{关键字}-dev.md`
4. `.kiro/steering/{关键字}-agreement.md`

**⚠️ 严格约束：**
- 文件名**必须严格**按照 `{关键字}-product.md`、`{关键字}-structure.md`、`{关键字}-dev.md`、`{关键字}-agreement.md` 格式命名，不得使用其他名称
- 例如用户提供关键字 `myapp`，则文件名必须是 `myapp-product.md`、`myapp-structure.md`、`myapp-dev.md`、`myapp-agreement.md`
- **禁止**复用、编辑或修改 `.kiro/steering/` 下已有的其他文件（如 `product.md`、`structure.md` 等）
- **禁止**创建上述四个文件以外的任何文件
- **禁止**修改项目中 `.kiro/steering/` 以外的任何文件

## 需要生成的文件内容

### 1. `{关键字}-product.md`

产品概述文档，front-matter 设置为 `inclusion: always`。

需要通过阅读项目代码（README、路由、页面组件、API 等）来总结以下内容：

- 项目/产品名称及简要介绍
- 核心功能模块列表（逐条列出，每条附简短说明）
- 支持的环境（如 dev、fat、uat、pre、pro 等）
- 集成的外部系统或服务（如 CI/CD、代码管理、通知、认证等）
- 界面语言
- 主要用户角色

参考风格：简洁的列表式描述，避免冗长段落。

### 2. `{关键字}-structure.md`

项目结构文档，front-matter 设置为 `inclusion: always`。

需要通过浏览项目目录结构来总结以下内容：

- 使用树形结构（`├──`、`└──`）展示主要目录和关键文件，每项附 `#` 注释说明用途
- 只展示有意义的目录层级，不需要列出每一个文件
- 在树形结构之后，补充一个「关键模式」小节，说明项目中的重要约定，例如：
  - 组件自动注册规则
  - API 调用方式
  - 路由组织方式
  - 路径别名
  - 状态管理方式
  - 其他值得注意的工程约定

参考风格：代码块内的树形结构 + 列表式的关键模式说明。

### 3. `{关键字}-dev.md`

开发约定文档，front-matter 设置为 `inclusion: always`。

需要通过阅读 package.json、构建配置、代码风格配置等来总结以下内容：

- 技术栈（框架、UI 库、构建工具、包管理器等，附版本号）
- 开发环境要求（Node 版本、包管理器等）
- 常用开发命令（启动、构建、lint 等）
- 代码风格与规范（ESLint 配置要点、命名约定等）
- CSS/样式方案（预处理器、全局变量、组件库样式覆盖等）
- 其他开发约定（如环境变量管理、代理配置等）

参考风格：分小节组织，每节用列表罗列要点。

### 4. `{关键字}-agreement.md`

项目约定文档，front-matter 设置为 `inclusion: always`。

**特殊规则：**
- 如果 `.kiro/steering/{关键字}-agreement.md` 文件**不存在**，则新建该文件，仅写入 front-matter（`inclusion: always`）和一级标题 `# 项目约定`，不添加其他内容
- 如果该文件**已存在**，则**不做任何修改**，直接跳过

## 执行规则

1. **仅操作上述四个文件**，不得读写、编辑、删除任何其他文件
2. 生成前先充分阅读项目代码，确保内容准确反映实际工程
3. 如果目标文件（同名文件）已存在，则在现有内容基础上优化和改进，保持原有结构和风格，尽量做增量修改而非重写（除非用户明确要求重写）
4. 如果目标文件不存在，则新建文件
5. 生成的文档使用中文，技术术语可保留英文
6. 四个文件全部处理完毕后，给出简要的完成总结
